var randomstring = require("randomstring");

module.exports = function (G) {

    // TODO: Don't forget to rename folder to auth

    var exports = {
        tayrProp: 'T',
        usersTable: 'user',
        sessionTable: 'session',
        identifier: 'username',
    };

    return exports;
};
